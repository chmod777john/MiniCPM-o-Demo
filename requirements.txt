
# pip install -r requirements.txt

--extra-index-url https://download.pytorch.org/whl/cu126

# Core ML
setuptools==83.0.0
torch==2.8.0+cu126
torchvision==0.23.0+cu126
torchaudio==2.8.0+cu126
transformers==5.5.4
accelerate==1.14.0
safetensors==0.8.0
triton==3.4.0

# Qwen3.5-MoE runtime acceleration with pure-Python/Triton packaging.
# Extra CUDA extension accelerators live in requirements-accel.txt.
flash-linear-attention==0.5.0
fla-core==0.5.0

# MiniCPM-o 4.6 / o5
# minicpmo-utils 1.0.6 metadata pins librosa==0.9.0, but the demo/gateway
# runtime is validated with newer librosa. install.sh installs minicpmo-utils
# separately with --no-deps so this explicit constraint wins.
pillow==10.4.0
decord==0.6.0
moviepy==2.1.2
onnxruntime==1.21.0
onnx==1.22.0
HyperPyYAML==1.2.3
einops==0.8.1

# Web Service
fastapi>=0.128.0
uvicorn>=0.40.0
httpx>=0.28.0
websockets>=16.0
python-multipart

# Docs rendering
markdown>=3.6
Pygments>=2.18.0

# Audio / Preset (gateway 也直接用:读 preset YAML、加载参考音频)
# 这些过去靠 minicpmo-utils[all] 传递依赖隐式提供,未显式声明;gateway 轻镜像不装
# minicpmo-utils,故在此显式列出。librosa 必须 >=0.10.2 —— 0.9.x 在新 setuptools 下
# 因缺 pkg_resources 导入即崩(见历史故障:preset 加载失败 → sys-prompt 入口消失)。
PyYAML==6.0.3
librosa>=0.10.2
soxr==1.1.0
soundfile==0.12.1
edge-tts>=7.2.0

# Data / Schema
pydantic==2.13.4
numpy==2.2.5

# Utilities
tqdm>=4.67.0

# Dev / Test
pytest>=9.0.0
pytest-asyncio>=1.3.0
