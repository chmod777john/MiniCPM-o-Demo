# pip install -r requirements-high.txt

# High-version runtime for Qwen3.5-MoE grouped-mm experiments.
# This is intentionally separate from requirements.txt, which tracks the
# validated o5 demo/runtime environment.  Use this with .venv-high only.

--extra-index-url https://download.pytorch.org/whl/cu130

# Core ML: newer torch exposes torch.nn.functional.grouped_mm on SM80+.
setuptools<82
torch==2.11.0+cu130
torchvision==0.26.0+cu130
torchaudio==2.11.0+cu130
torchcodec==0.11.1+cu130
transformers==5.13.0
accelerate>=1.14.0
safetensors>=0.8.0
triton>=3.5.0

# Qwen3.5-MoE runtime acceleration with pure-Python/Triton packaging.
flash-linear-attention>=0.5.0
fla-core>=0.5.0

# Optional CUDA extension accelerators are intentionally not pinned here.
# Install them separately only when a matching wheel is available:
#   flash-attn
#   causal-conv1d

# MiniCPM-o / media runtime
pillow>=10.4.0
decord==0.6.0
moviepy>=2.1.2
onnxruntime>=1.21.0
onnx>=1.22.0
HyperPyYAML>=1.2.3
einops>=0.8.1

# Web service
fastapi>=0.128.0
uvicorn>=0.40.0
httpx>=0.28.0
websockets>=16.0
python-multipart

# Docs rendering
markdown>=3.6
Pygments>=2.18.0

# Audio / presets
PyYAML>=6.0.3
librosa>=0.10.2
soxr>=1.1.0
soundfile>=0.12.1
edge-tts>=7.2.0

# Data / schema
pydantic>=2.13.4
numpy>=2.2.5

# Utilities
tqdm>=4.67.0

# Dev / probes
pytest>=9.0.0
pytest-asyncio>=1.3.0
