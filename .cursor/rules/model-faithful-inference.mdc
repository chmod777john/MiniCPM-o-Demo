---
description: 最高优先级：Demo 推理必须忠实反映 checkpoint 能力，禁止用推理补丁制造效果
alwaysApply: true
---

# 模型忠实推理最高优先级准则

Demo 推理是模型能力的测量仪器，不是能力补偿器。模型效果不佳时，默认结论必须回到
训练、数据或 checkpoint；不得为了展示效果修改推理语义。

## [MUST NOT] 禁止用推理制造能力

- 不增加 speak/listen/tool-call logit bias、强制 token、阈值或启发式 fallback。
- 不伪造、补写、重映射或吞掉模型生成的协议 token 来改善展示。
- 不因主观体验修改 KV 序列、slot 顺序、停止条件、采样策略或 prompt 内容。
- 不把模型未调用的工具改成规则触发，也不把普通文本包装成 think/tool-call。
- 不以“效果更好”为理由把实验补丁放进默认部署路径。

## [MUST] 允许修复的边界

仅当证据证明实现违反已定义的训练/SDK/模型契约时，才能修推理代码。修复前必须：

1. 固定 checkpoint、输入、seed、decode mode 和原始基线。
2. 以训练 token 序列、SDK parser 和 O45 正确实现逐层对拍。
3. 找到首个确定性分叉点，证明不是模型原始 logits/输出行为。
4. 使用最小改动，并验证修复前后非目标 token/logits/KV 不变。

观测、日志、trace、HTTPS、进程生命周期和启动预热可以修改，但必须证明不改变模型输入、
logits、采样结果和协议 token 序列。

## [MUST] 效果问题的处理

如果模型原始输出合法但效果差、margin 低、选择 listen/no_action 或漏工具：

- 原样记录输出和 margin；
- 将问题归入训练数据、目标设计、checkpoint 或模型能力；
- 通过训练和评测 Gate 改善；
- 禁止在推理侧“修到看起来成功”。

任何确需改变推理语义的实验必须先获得用户明确批准，放在独立实验分支/Profile 中，
不得替换忠实基线。
